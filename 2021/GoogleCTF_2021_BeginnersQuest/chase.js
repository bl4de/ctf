const turnLeft = function () {
    return -1;
};

const turnRight = function () {
    return 1;
};

const goStraight = function () {
    return 0;
};

// indicates if lane is empty
let left = false;
let center = false;
let right = false;

const MINIMUM_SAFE_DISTANCE = 10;

for (let i in scanArray) {
    // index 8 is CENTER
    
}
